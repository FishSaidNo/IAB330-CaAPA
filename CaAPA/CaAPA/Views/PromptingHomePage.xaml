<?xml version="1.0" encoding="UTF-8"?>
<BaseView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="CaAPA.PromptingHomePage">
	<StackLayout Padding = "10,20,10,30" Orientation="Vertical" >

		<Label Text="Prompting"
			VerticalOptions="Center"
			FontAttributes="Bold"
			FontSize="Large"
			TextColor="Black"
		/>

		<ListView x:Name="BeaconListView" ItemsSource="{Binding Data, Mode=TwoWay}">
<!--			<ListView.ItemTemplate>
				<DataTemplate>
							<ViewCell>
								<Grid>
									<Grid.RowDefinitions>
										<RowDefinition Height="1*"/>
										<RowDefinition Height="1*"/>
									</Grid.RowDefinitions>
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="2*"/>
										<ColumnDefinition Width="1*"/>
									</Grid.ColumnDefinitions>
									<Label Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding TimeStamp}"/>
									<Label Grid.Row="0" Grid.Column="1" Text="{Binding ActionRequiredFlag}" />
									<Label Grid.Row="0" Grid.Column="0" Text="{Binding Name}"/>
								</Grid>
							</ViewCell>
						</DataTemplate>
			</ListView.ItemTemplate>-->
		</ListView>

		<Button HorizontalOptions = "FillAndExpand" Text="Add New Prompt" Command = "{Binding AddNewPromptCommand}"/>

	</StackLayout>
</BaseView>
