<?xml version="1.0" encoding="UTF-8"?>
<BaseView xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="CaAPA.PromptingHomePage">
	<StackLayout Padding = "10,20,10,30" Orientation="Vertical" >

		<Label Text="Prompting"
			VerticalOptions="Center"
			FontAttributes="Bold"
			FontSize="Large"
			TextColor="Black"
		/>

		<ListView x:Name="BeaconListView" ItemsSource="{Binding Data, Mode=TwoWay}" ItemTapped="OnItemSelected">
<!--			<ViewCell.ContextActions>
	            <MenuItem Clicked="OnComplete" CommandParameter="{Binding .}"
	               Text="Complete" IsDestructive="True" />
	        </ViewCell.ContextActions>-->
		</ListView>
	</StackLayout>
</BaseView>
